/*
元素节点：
除Document以外，Element也非常常用。
Element提供了对元素标签名、子节点或特性的访问
用元素.tagName() 可以返回标签名


1)
获取/新增/移除元素属性

ele.getAttribute('name')
ele.setAttribute(name.value)
ele.removeAttribute(name)

2)创建元素
document.createElement(小写标签名)，返回所生成的DOM元素

*/