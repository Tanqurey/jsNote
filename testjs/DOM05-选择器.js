/*
querySelector(selector) 返回满足选择器条件的头一个元素
querySelectorAll(selector) 返回满足选择器条件的所有元素组成的nodeList

元素遍历新增属性，不必担心文本节点的影响，只会返回元素

childElementCount：
返回的是子元素，不包括文本节点和注释的个数

.firstElementChild 指向第1个子元素
.lastElementChild 指向最后1个子元素
.previousElementSibling 前一个子元素
.nextElementSibling 后一个子元素


*/